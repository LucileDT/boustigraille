{% extends 'base.html.twig' %}

{% block title %}Édition de la recette {{ recipe.name }}{% endblock %}

{% block breadcrumb %}
    <li class="breadcrumb-item">
        <a href="/">Accueil</a>
    </li>
    <li class="breadcrumb-item">
        <a href="#">Recettes</a>
    </li>
    <li class="breadcrumb-item" aria-current="page">
        <a href="{{ path('recipe_index') }}">Liste des recettes</a>
    </li>
    <li class="breadcrumb-item active" aria-current="page">
        Édition de la recette {{ recipe.name }}
    </li>
{% endblock %}

{% block body %}
    <div class="row mt-5 mb-4">
        <div class="col">
            <h1>Édition de la recette {{ recipe.name }}</h1>
        </div>
    </div>

    <div class="row">
        <div class="col">
            {{ include('recipe/_form.html.twig', {'button_label': 'Update'}) }}
        </div>
    </div>
{% endblock %}
