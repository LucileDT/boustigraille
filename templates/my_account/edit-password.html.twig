{% extends 'base.html.twig' %}

{% block title %}Éditer mon mot de passe{% endblock %}

{% block page_title %}
    <div class="container-fluid mt-3">
        <div class="row">
            <div class="col">
                <div class="page-heading">
                    <h6>
                        <a href="{{ path('home') }}">Accueil</a>
                        /
                        <a href="{{ path('my_account_index') }}">Mon compte</a>
                        / Éditer mon mot de passe
                    </h6>
                    <h2>
                        Éditer mon mot de passe
                    </h2>
                </div>
            </div>
        </div>
    </div>
{% endblock %}


{% block body %}
    <div class="row">
        <div class="col">
            {{ form_start(form) }}
            {{ form_widget(form) }}
                <button class="btn btn-primary">{{ button_label | default('Mettre à jour') }}</button>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
